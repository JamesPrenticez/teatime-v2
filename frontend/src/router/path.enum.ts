export enum Path {
  HOME = '/', // redirects to dashboard
  
  // Auth
  AUTH = '/auth',
  LOGIN = 'login',
  REGISTER = 'register',

  // Dashboard
  DASHBOARD = '/dashboard',
  SURVEYS = 'surveys',
}